/*global angular */

(function () {
    'use strict';

    var serviceModules = [];

    angular.module('frog.resources', serviceModules)
        .constant('frogResources', {
            /* Strings are defined in separate JSON files located in js/sky/locales. */
        });
}());